{
    "suggest": {
        "prefix": {{ searchTerm|json_encode|raw }},
        "completion": {
            "field": "suggest",
            "size": {{ size|json_encode|raw }},
            "skip_duplicates": true,
            "fuzzy": {
                "fuzziness": 1,
                "transpositions": true,
                "min_length": 4
            }
        }
    }
}
