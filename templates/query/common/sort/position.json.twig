{
    "taxons.position": {
        "order": {{ order|json_encode|raw }},
        "nested": {
            "path": "taxons",
            "filter": {
                "term": {
                    "taxons.sylius-id": {{ taxon.id|json_encode|raw }}
                }
            }
        }
    }
}
