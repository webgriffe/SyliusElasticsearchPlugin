{
    "name.value": {
        "order": "{{ order }}",
        "nested": {
            "path": "name",
            "filter": {
                "match": {
                    "name.locale": "{{ localeCode }}"
                }
            }
        }
    }
}
