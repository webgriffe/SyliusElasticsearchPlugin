{
    "suggest": {
        "text": {{ searchTerm|json_encode|raw }},
        "term": {
            "field": {{ ('name.' ~ localeCode)|json_encode|raw }},
            "suggest_mode" : "missing"
        }
    }
}
